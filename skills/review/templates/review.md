# 评审：<产物> · 第 <N> 轮

> 由独立上下文的评审模型产出。先给结论，再按下表逐条记录发现项。

## 结论

<一句话说清这版产物能否进入下一步。只要还有「高危」未解决，就是还不能进入——不写「基本合格」「有条件通过」这类中间话。>

## 发现项

| #   | 严重性 | 审查项           | 位置                         | 问题（附证据）                                   | 处理方式             |
| --- | ------ | ---------------- | ---------------------------- | ------------------------------------------------ | -------------------- |
| 1   | 高危   | C. 可实施性      | `plan.md` §4.1               | 写操作未定义幂等键，重试会重复扣款               | 本层修复             |
| 2   | 高危   | A. 状态覆盖度    | 订单列表页 · 空态            | PRD 要求空态引导，设计稿缺失                     | 交用户拍板           |
| 3   | 中危   | B. AC 覆盖       | `src/services/export.ts:88`  | AC-004 要求完成通知，但 spec 未定义通知渠道，实现无从对照 | backtrack 回 clarify |

**取值说明：**

- **严重性**：`高危`（拦路，必须先解决才能进下一步）/ `中危`（有风险，不拦路）/ `低危`（建议优化）。
- **审查项**：填审查项提示词里对应的章节（如 `C. 可测试性`）；没有给定提示词时，填自拟的维度名。
- **位置**：指到能直接找到的粒度——文档用章节 / 条目编号，代码用文件:行号，设计稿用页面 + 状态。
- **问题**：必须附证据——文档引原文，代码贴行号，设计稿指明具体位置与对应的 PRD / spec 条目。不接受凭印象的结论。
- **处理方式**：`本层修复`（作者自修）/ `交用户拍板`（需人决策）/ `backtrack 回 <层>`（根在上游）。**方向性问题——产物忠于上游、却暴露了需求 / 设计 / 方案本身的缺陷——一律不在本层硬补，走后两种去向。**

## 已检查无异常

<一句话列出已审、无异常的维度，不逐条展开。>
