---
name: implementing-plans
description: 照定稿的技术方案施工，产出代码与测试。适用于：方案定稿后的编码阶段——功能实现、缺陷修复、技术改造都用它。
argument-hint: "<技术方案路径或改动说明>"
---

# 照方案施工

按定稿的技术方案把代码写出来。编码纪律是测试驱动的（见 test-driven-development）：每条验收依据先有失败的测试，实现让它转绿——代码写完的时候，证据也同时写完了。

## 工作流程

### 1. 读齐输入

- 技术方案：实施步骤、影响面、接口与数据模型的定义。
- 验收依据：需求的验收标准、缺陷的复现记录与修复预期，或技术任务的完成标准。
- 若项目里维护着长期规约文档（命名、目录、错误处理、测试范式），以它为准绳；没有的话，遵循代码库的既有风格。

### 2. 按实施步骤推进

- 照方案的实施步骤依次执行，每步完成时对一下方案里写的验证点。
- 每条验收依据走一轮 test-driven-development 的红绿循环；缺陷修复的第一轮固定是把复现步骤固化成失败测试。
- 自动化不划算的验证（纯视觉效果、依赖真实外部系统），记下手动验证方式，留给测试阶段执行。
- 改动宽而同质时（几十处同型修改），若环境支持并行执行，可以把任务分发出去同时处理，每份带同一段改法说明和验证命令，收口时逐份核对。链式依赖的步骤不这么干——连贯比隔离值钱。

### 3. 方案盖不住时，停

- 实现中发现方案错了或盖不住新情况——接口走不通、影响面比方案写的大——停下来改方案，方案重新走评审，不在代码里悄悄绕开。
- 对验收依据本身有疑问，退回给定它的人澄清，不按自己的理解顺手改。

### 4. 自查与收尾

- 全量测试跑一遍：新测试全绿，既有测试没被弄坏。
- 把 diff 从头读一遍：调试痕迹（打印语句、临时注释、试验代码）清干净；不属于本次改动的「顺手改进」撤掉。
- 逐条对照验收依据：每条要么有测试引用，要么有记录在案的手动验证方式。

## 完成的定义

- 每条验收依据有对应的测试，或明确的手动验证方式。
- 全量测试通过。
- diff 里的每一行都能追溯到方案。

## 注意事项

- 只动方案影响面之内的东西。顺手升级依赖、重排 import、「优化」路过的代码，都是在给评审制造噪音。
- 测试过不了，改代码；确实是验收依据错了，退回定它的人，不自己顺手改。
- 遇到「设计如此还是缺陷」拿不准的现状，提出来问，不悄悄按自己的理解处理。
