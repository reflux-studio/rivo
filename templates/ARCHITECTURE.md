# 技术架构文档

> 技术架构文档：系统怎么搭起来的、为什么。
> 按需取用下列小节，不适用的整段省略，别为凑齐结构编造内容。ASCII 图能画就画。

## 总览 / 上下文

<系统技术定位、边界；与哪些外部系统 / 角色交互>

```
[ASCII 上下文图：本系统 + 外部依赖]
```

## 组件与职责

<主要模块 / 服务，各自负责什么>

```
[ASCII 组件图：方框 + 依赖箭头]
```

## 依赖与分层

<谁能依赖谁、调用方向、分层规则（属不变量的一部分）>

## 数据流

<关键路径上数据怎么进来、经过什么转换、从哪出去（请求生命周期 / 事件流）>

```
[ASCII 数据流图]
```

## 数据与存储

<持久化实体、状态落在哪、存储技术、粗粒度数据模型>

## 横切关注（按需）

<鉴权 / 错误处理 / 日志 / 配置 / 并发，架构层怎么统一处理>

## 对外接口（按需）

<暴露的 API、第三方集成、协议>

## 关键决策与不变量

<必须守住的规则 + 理由 + 否掉的备选（轻量 ADR）；如「UI 层不直连 DB」>

## 目录骨架

<顶层区域各管什么，让改动知道往哪放；不穷举文件>

## 已知债 / 约束

<刻意欠的债、扩展性边界、待重构项>
