# <需求名称> UI 规格（ui-spec）

> ui-spec：本次需求的 UI 意图。看设计稿的过程就是写它的过程：组件怎么组合、有哪些状态、来源在哪。
> 全局设计系统（token / 组件库 / 模式）在 DESIGN.md，这里不重复，只写本次。
> 不写组件 Props 类型（归 plan）、不写代码。仅前端需求才有。

## 视图清单

> 本次新增 / 改动哪些屏。

- ……

## 审稿结论

> 设计稿对 spec 的核对结果。无设计稿时写「无稿，直接设计」。

- **覆盖缺口**：[spec 要、稿没画的状态 / 场景] → Framer / 人
- **越界**：[稿有、产品没要的] → Framer 确认

---

## 视图：<视图名>

- **设计稿来源**：Figma 节点 `xxx` / 链接 / 本地 `assets/xxx.html`（实现时回查这里取精确样式）
- **目的与入口**：这屏干什么、用户怎么进来
- **组件组合**：
  - 复用：`<DataTable>`、`<FilterBar>`（见 DESIGN.md）
  - 新增 `<ExportButton>`：用途…；现有的为什么替代不了…（仅意图，Props 类型归 plan）
  - 布局 / 组件树：页面 → 区块 → 组件
- **状态**：正常 / 空 / 加载 / 错误 / 无权限 / 边界，各态长什么样、用哪个组件态
- **交互与流转**：关键操作 → 反馈 → 状态变化
- **本次文案**：CTA / 空态 / 错误的具体文案（全局语气在 DESIGN.md）
- **视觉偏离**：哪里偏离 DESIGN.md 默认、为什么

> 多个视图就复制上面这块。
